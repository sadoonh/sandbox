"""{{ description }}"""

OWNER = "{{ owner }}"
OUTPUT_TABLES = [{{ output_tables }}]


def main() -> None:
    from sandbox import io

    # Example: read source data, transform, write result
    # df = io.query("SELECT * FROM source_db.your_table WHERE ...")
    # result = df.groupby("id").agg(...)
    # io.write_table(result, "{{ first_table }}")
    pass
